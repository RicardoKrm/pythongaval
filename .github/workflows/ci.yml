name: Django CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:13
        env:
          POSTGRES_DB: test_tms_gaval_db
          POSTGRES_USER: tms_user
          POSTGRES_PASSWORD: karma627
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.12
      uses: actions/setup-python@v2
      with:
        python-version: 3.12
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Run Tests
      env:
        SECRET_KEY: 'django-insecure-test-secret-key'
        DEBUG: True
        DB_NAME: test_tms_gaval_db
        DB_USER: tms_user
        DB_PASSWORD: karma627
        DB_HOST: localhost
        DB_PORT: 5432
      run: |
        python manage.py test flota
